name: Version GitTooling

on:
  push:
    branches: [main]
    paths:
      - '**'
      - '!index.html'
      - '!README.md'
      - '!LICENSE'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  version:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the exact main commit
        uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Determine the immutable GitTooling version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          version="$(bash .github/scripts/bepaal-versie-gittooling.sh "$GITHUB_SHA")"
          printf 'value=%s\n' "$version" >> "$GITHUB_OUTPUT"
          printf 'VERSION=%s\n' "$version" >> "$GITHUB_ENV"
          echo "GitTooling version for ${GITHUB_SHA}: ${version}"

      - name: Tag the exact main commit
        shell: bash
        run: |
          set -euo pipefail
          existing_sha="$(git rev-list -n 1 "$VERSION" 2>/dev/null || true)"
          if [ -n "$existing_sha" ]; then
            if [ "$existing_sha" != "$GITHUB_SHA" ]; then
              echo "::error::Tag $VERSION already points to $existing_sha instead of $GITHUB_SHA"
              exit 1
            fi
            echo "$VERSION already points to this commit"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$VERSION" "$GITHUB_SHA" -m "GitTooling $VERSION"
          if git push origin "refs/tags/$VERSION"; then
            exit 0
          fi

          git fetch --force origin "refs/tags/$VERSION:refs/tags/$VERSION"
          remote_sha="$(git rev-list -n 1 "$VERSION")"
          if [ "$remote_sha" != "$GITHUB_SHA" ]; then
            echo "::error::Remote tag $VERSION points to $remote_sha instead of $GITHUB_SHA"
            exit 1
          fi
          echo "$VERSION was created by the parallel run and points to the expected commit"

      - name: Create the GitHub release once
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          if gh release view "$VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $VERSION already exists"
            exit 0
          fi
          if gh release create "$VERSION" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "GitTooling $VERSION" \
            --generate-notes; then
            exit 0
          fi
          if gh release view "$VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $VERSION was created by the parallel run"
            exit 0
          fi
          echo "::error::Release $VERSION could not be created or verified"
          exit 1
